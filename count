b, c, d, k = [], [], 0, 0
для я в:
    с = 0
    для j в i:
        c + = int (j)
    b.append (c / 3)
печать (* b, sep = '\ n')
для i в диапазоне (len (a)):
    d + = int (a [i] [0])
k.append (d / 3)
d = 0
для i в диапазоне (len (a)):
    d + = int (a [i] [1])
k.append (d / 3)
d = 0
для i в диапазоне (len (a)):
    d + = int (a [i] [2])
k.append (d / 3)
d = 0
печать (* k)
